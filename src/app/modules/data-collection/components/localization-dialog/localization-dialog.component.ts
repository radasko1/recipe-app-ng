import { Component, ViewEncapsulation } from '@angular/core';

@Component({
  selector: 'app-localization-dialog',
  template: ` <p>localization-dialog works!</p> `,
  encapsulation: ViewEncapsulation.None,
})
export class LocalizationDialogComponent {}
